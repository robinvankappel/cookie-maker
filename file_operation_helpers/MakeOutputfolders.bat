SET dir=D:\db-filler\generated_scripts\OUTPUT_results\

if not exist %dir%A mkdir %dir%A
if not exist %dir%B mkdir %dir%B
if not exist %dir%C mkdir %dir%C
if not exist %dir%D mkdir %dir%D
if not exist %dir%E mkdir %dir%E
if not exist %dir%F mkdir %dir%F
if not exist %dir%G mkdir %dir%G
if not exist %dir%H mkdir %dir%H
if not exist %dir%I mkdir %dir%I
if not exist %dir%J mkdir %dir%J
